@using DexCMS.Core.Infrastructure;
@using DexCMS.Tickets.Mvc.Extensions;

@if (SiteSettings.Resolve.GetSetting("ShowPublicEvents").ToLower() == "true")
{
<li class="submenu">
    <a href="#">Events <span class="fa"></span></a>
    @Html.BuildEventList(ViewBag.Events)
</li>
}